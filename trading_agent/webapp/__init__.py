"""Browser-based web UI for the trading agent — FastAPI + WebSocket
backend (session.py, server.py) driving a dependency-free vanilla-JS
frontend (static/index.html). Optional: needs requirements-web.txt,
not used by the CLI or anything else in this project.

See session.py's module docstring for the execution model.
"""
