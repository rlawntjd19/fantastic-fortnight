# Optional extra for the browser-based web UI (trading_agent/webapp/).
# Not needed for the CLI or anything else in this project.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
# Only needed to run tests/test_webapp_server.py (fastapi.testclient.TestClient).
httpx>=0.27.0
